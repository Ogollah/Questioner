language: python
python:
  - "3.6"

# command to install dependencies
install:
  - pip3 install pytest-cov
  - pip3 install coveralls
  - pip3 install -r requirements.txt
# command to run tests
script:
   - pytest
   - coverage run --source=app -m pytest && coverage report - coverage run --source=app -m pytest && coverage report 

after_script:
  - coveralls

cache: pip3
